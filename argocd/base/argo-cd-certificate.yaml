apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argo-cd-cert
spec:
  secretName: argocd-tls
  issuerRef:
    name: argo-cd-issuer
    kind: Issuer
  commonName: cd.apps.containerly.io
  dnsNames:
  - cd.apps.containerly.io
